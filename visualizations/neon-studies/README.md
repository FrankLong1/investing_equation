# Neon studies

The original five Three.js studies, grouped as one visual family: dark backgrounds, glowing wireframes, floating boxes, and spatial navigation. Each study still owns its implementation and reads the root documents directly.

Open this family from the main gallery, or run `npm run dev` from `visualizations/`.
